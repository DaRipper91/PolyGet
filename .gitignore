__pycache__/
*.py[cod]
*$py.class
*.so
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.DS_Store
*.log

# rip-it-apart scratch state — intermediate stage reasoning, not the deliverable.
# The final fix-plan file lands in docs/plans/ and is NOT ignored.
.audit/

# Local Claude Code settings (personal overrides, not shared)
.claude/settings.local.json
